const config =  {
 clearMocks: true,
 collectCoverage: true,
 coverageDirectory: "coverage",
 coverageProvider: "v8",
 verbose: true,
  // setupFilesAfterEnv: [""],
  testTimeout: 15000
};

export default config;
